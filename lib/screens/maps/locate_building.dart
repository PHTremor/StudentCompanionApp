import 'package:flutter/material.dart';

class LocateBuildingScreen extends StatelessWidget {
  const LocateBuildingScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Taona Google map Ndidza Msanga!"),
    );
  }
}
