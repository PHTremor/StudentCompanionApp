import 'package:flutter/material.dart';

class PeerClubsScreen extends StatelessWidget {
  const PeerClubsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text("Taona Peer Ndidza Msanga!"),
      ),
    );
  }
}
